# Global settings
set -g default-terminal "screen-256color"
set -g mouse on

# Remap prefix key
unbind-key C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Bindings
bind - split-window -v
bind-key -n C-k send-keys -R \; send-keys "Enter"
bind-key -n C-w kill-session
